@using DevExtremeAspNetCoreApp1.Models
<h2 class="content-block">Product From Api</h2>



@(Html.DevExtreme().DataGrid<Product>()
    .ElementAttr(new { @class = "dx-card wide-card" })
    .DataSource(d => d.Mvc().Controller("Home").LoadAction("ProductApiData").Key("ProductId"))
    .ShowBorders(false)
    .FilterRow(f => f.Visible(true))
    .FocusedRowEnabled(true)
    .FocusedRowIndex(0)
    .ColumnAutoWidth(true)
    .ColumnHidingEnabled(true)
    .Columns(columns => {

        columns.AddFor(m => m.ProductName);
        columns.AddFor(m => m.UnitPrice);
        columns.AddFor(m => m.UnitsInStock);
        columns.AddFor(m => m.ReorderLevel);
    })
    .Paging(p => p.PageSize(10))
    .Pager(p => p
        .ShowPageSizeSelector(true)
        .AllowedPageSizes(new[] { 5, 10, 20 })
        .ShowInfo(true)
    )
)